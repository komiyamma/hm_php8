# プロジェクトの問題点

このドキュメントは、プロジェクトにおける現在の問題点と改善の可能性のある領域をまとめたものです。

## 1. 深刻なコードの重複と乖離

### 問題点
`ext/hidemaru` と `HmPHP8/hmPHP8` の2つのディレクトリ間で、多くのC++ソースファイルがコピーされています。これらのファイルは単純な複製ではなく、内容が異なる（乖離している）場合があります。

- **例:** `dllfunc_interface.cpp` は `ext/hidemaru` ではほぼ空ですが、`HmPHP8/hmPHP8` には完全な実装が含まれています。

この状態は、バグ修正や機能追加の際に、両方のファイルを同期させる必要があり、非常にエラーが発生しやすく、メンテナンスコストを著しく増大させます。

### 提案
- コードを共有するための一元的な場所（例: `src/common`）を設け、両方のプロジェクトから参照するようにします。
- プロジェクトの依存関係を整理し、片方がもう片方をライブラリとして利用する構成に変更します。

## 2. 複雑で関連性のないビルドシステム

### 問題点
プロジェクトには2つの独立したビルドシステムが存在します。
- `ext/hidemaru`: PHP拡張機能の標準的なビルド方法である`config.m4` (PECL) を使用します。
- `HmPHP8`: Visual Studioのソリューション (`.sln`) とプロジェクトファイル (`.vcxproj`) を使用します。

これにより、プロジェクト全体のビルド手順が複雑になり、新規開発者がビルドプロセスを理解するのが困難になっています。

### 提案
- ビルドプロセスを自動化するスクリプト（例: PowerShell, バッチファイル）を作成し、両方のコンポーネントを一度にビルドできるようにします。
- 可能であれば、CMakeのようなクロスプラットフォームなビルドシステムを導入し、ビルド定義を一本化します。

## 3. 依存関係管理の欠如

### 問題点
2つのC++プロジェクト (`php_hidemaru.dll` と `hmPeach.dll`) 間で共有されるべきコードが、ファイルのコピーによって管理されています。

### 提案
- 共有コードを静的ライブラリ (`.lib`) として分離し、両方のプロジェクトがそのライブラリにリンクするようにします。これにより、コードの重複が解消され、依存関係が明確になります。

## 4. 一貫性のないファイルエンコーディング

### 問題点
C++のソースファイルがUTF-16 LEでエンコードされています。これはC++ソースコードとしては非標準であり、`git diff` や `grep` などの標準的な開発ツールとの相性に問題を引き起こす可能性があります。

### 提案
- すべてのソースコードファイルを標準的なUTF-8エンコーディングに統一します。

## 5. モダンなC++プラクティスの欠如

### 問題点
コードは古いC++のスタイルで書かれています。
- 生ポインタや手動のメモリ管理が使われている可能性があります。
- Cスタイルの文字列操作や手動でのバッファ管理が見られます。

### 提案
- スマートポインタ (`std::unique_ptr`, `std::shared_ptr`) を活用し、メモリリークのリスクを低減します。
- 範囲ベースforループや `auto` キーワードなどを活用し、コードの可読性と保守性を向上させます。

## 6. テスト不足

### 問題点
`ext/hidemaru/tests` には3つの `phpt` テストファイルしか存在しません。プロジェクトの複雑さを考えると、テストカバレッジが著しく不足していると考えられます。

### 提案
- PHPのテストフレームワーク（PHPUnitなど）を導入し、より多くのテストケースを追加します。
- C++側のロジックに対しても単体テスト（Google Testなど）を導入することを検討します。

## 7. ドキュメントとコメントの不足

### 問題点
- C++コード内の日本語コメントが文字化けしており、内容を理解できません。
- `hmPeach.dll` と `php_hidemaru.dll` の関係性や、プロジェクト全体のアーキテクチャを説明するドキュメントが存在しません。

### 提案
- ファイルエンコーディングをUTF-8に修正し、コメントが正しく読めるようにします。
- `README.md` やアーキテクチャ図などで、プロジェクトの全体像と各コンポーネントの役割を明確に記述します。

## 8. 紛らわしい命名規則

### 問題点
プロジェクト、ディレクトリ、生成されるDLLの名前が一貫していません。
- プロジェクト名: `hmPeach`
- PHP拡張: `hidemaru`
- DLL: `hmPeach.dll`
- ソリューション/プロジェクト: `HmPHP8`

これは混乱を招く原因となります。

### 提案
- プロジェクト全体の命名規則を統一し、各コンポーネントの名前がその役割を明確に表すようにリネームします。

## 9. ビルド成果物のバージョン管理への混入

### 問題点
リポジトリにビルド成果物 (`.obj`, `.dll`, `.pdb`) や、個人のIDE設定ファイル (`.vcxproj.user`) が含まれています。これらはバージョン管理に含めるべきではありません。

### 提案
- `.gitignore` ファイルをプロジェクトのルートに作成し、ビルド成果物や一時ファイル、IDE固有の設定ファイルなどをバージョン管理から除外します。
